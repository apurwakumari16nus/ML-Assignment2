# DSS5104 Assignment 2 — Reddit Mental Health NLP Pipeline
# Install:  python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# Python:   3.10+  (tested on 3.11, Apple Silicon macOS)

# ── Data handling ─────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
requests>=2.31          # Reddit public JSON API (01, 02)

# ── Lexicon sentiment (script 04) ─────────────────────────────
vaderSentiment>=3.3.2

# ── Classical ML baselines (10, 11, 15, 16) ───────────────────
scikit-learn>=1.3       # TfidfVectorizer, LogisticRegression, LinearSVC, metrics

# ── Deep-learning stack (05, 06, 07, 08, 09, 15, 16, 18, 20, 21, 22)
torch>=2.1              # MPS (Apple Silicon) / CUDA / CPU backends
transformers>=4.40      # BERT, DistilBERT, AutoTokenizer, Trainer, pipeline
datasets>=2.14          # dair-ai/emotion (Saravia) loader
accelerate>=0.27        # required by transformers.Trainer

# ── Statistics (17, 18, 21, 22) ───────────────────────────────
scipy>=1.11             # mannwhitneyu, chi2_contingency

# ── Plotting (12, 13, 14, 15, 16, 17, 18) ─────────────────────
matplotlib>=3.7
